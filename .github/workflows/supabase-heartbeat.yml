name: Supabase Heartbeat

# Run every day at 12:01 PM IST (06:31 UTC same day)
# IST = UTC + 5:30, so 12:01 PM IST = 06:31 AM UTC
on:
  schedule:
    - cron: '31 6 * * *'  # 06:31 UTC = 12:01 PM IST
  workflow_dispatch:  # Allow manual triggering

jobs:
  heartbeat:
    runs-on: ubuntu-latest
    
    steps:
      - name: Keep Supabase Active
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}
        run: |
          # Update heartbeat record to keep database active
          echo "Updating heartbeat in Supabase at: $SUPABASE_URL"
          
          response=$(curl -s -w "\n%{http_code}" \
            "$SUPABASE_URL/rest/v1/heartbeat?id=eq.1" \
            -X PATCH \
            -H "apikey: $SUPABASE_ANON_KEY" \
            -H "Authorization: Bearer $SUPABASE_ANON_KEY" \
            -H "Content-Type: application/json" \
            -H "Prefer: return=representation" \
            -d '{"last_ping": "now()"}')
          
          http_code=$(echo "$response" | tail -n1)
          body=$(echo "$response" | sed '$d')
          
          echo "HTTP Status Code: $http_code"
          echo "Response Body: $body"
          
          if [ "$http_code" = "200" ] || [ "$http_code" = "204" ]; then
            echo "✅ Heartbeat updated successfully in database"
            exit 0
          else
            echo "❌ Failed to update heartbeat. Status: $http_code"
            exit 1
          fi
      
      - name: Log Success
        if: success()
        run: echo "✅ Heartbeat completed successfully at $(date)"
      
      - name: Log Failure
        if: failure()
        run: echo "❌ Heartbeat failed at $(date)"
